cout << 을 할 때 문자를 출력하는데는 ' ' 와 " " 둘다 사용할 수 있지만 개행문자 즉 \n과 같이 쓴다면
" " 을 사용해야지 오류가 발생하지 않는다

배열을 선언할 때는 항상 초기화를 염두해라
e.g. int a[20] = {}; (O)
     int a[20];      (X)

물론 변수도 따로 입력받는 것이 아니라면 초기화를 염두해라
e.g. int sum = 0; (O)
     int sum;     (X)
     
